# Public site URL (used for canonical, OG, sitemap)
SITE_URL=https://power-of-ai-hub-astro.pages.dev

# Optional: OpenAI key for /tools/prompt-engineer generate + optimise modes.
# Adapt mode is fully client-side and does not require this key.
# When wiring backend support, expose this only inside Cloudflare Pages Functions,
# never to client JS.
# OPENAI_API_KEY=sk-...
